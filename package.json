{
  "version": "0.0.0",
  "devDependencies": {
    "del": "^3.0.0",
    "del-cli": "^1.1.0",
    "rollup": "^0.58.2"
  },
  "scripts": {
    "clear": "del es/ lib/",
    "prebuild": "npm run clear",
    "predev": "npm run clear",
    "build": "rollup -c",
    "dev": "rollup -c -w"
  },
  "files": [
    "lib",
    "es"
  ],
  "license": "ISC",
  "dependencies": {}
}
